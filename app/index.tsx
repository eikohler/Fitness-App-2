import Header from "@/components/Header";
import { StyleSheet, View } from "react-native";

export default function Workouts() {
  return (
    <View>
      <Header title={'Workouts'} subtext={'Week 3'} />
    </View>
  );
}
